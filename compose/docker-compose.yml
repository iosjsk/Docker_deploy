---
version: '3'
services:

 dbown:
   build: 
    context: .
    dockerfile: ../mysql/Dockerfile
   ports:
    - 6600:3306
   environment:
    MYSQL_ROOT_PASSWORD: test@1234
   restart: always

 mycent:
   image: centos:latest


 myapache:
  build:
   context: .
   dockerfile: ../apache/Dockerfile
  ports:
    - 9999:80
  depends_on:
    - dbown

